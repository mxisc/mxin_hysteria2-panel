# Minimal required runtime config
# `public_api_base_url` can be completed later from setup/system settings.
BIND_ADDR=127.0.0.1:18080
LOG_LEVEL=DEBUG
ENCRYPTION_KEY=change-this-to-a-random-secret
LOGIN_AES_SEED=change-this-to-a-random-seed

# Database
DB_HOST=127.0.0.1
DB_NAME=hy2_panel
DB_USER=root
DB_PASSWORD=change-me
